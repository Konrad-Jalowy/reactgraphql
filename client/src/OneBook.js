import { useState } from "react";
import { useLazyQuery, gql } from "@apollo/client";
function OneBook(){
    return (
        <>
        <p>Not implemented yet(onebook)</p>
        </>
    );
};

export {OneBook};